# GAM-DiD
This is a repository contains the core R codes for the GAM-DiD model for the W2 ERW project (data was generated for the model display). R version 4.3.3 was used for all the analysis.  Run the 00C_main script.R script to load and analyse the data.
